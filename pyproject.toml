[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "loglens"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["click"]

[project.scripts] 
loglens = "loglens.cli:analyze"